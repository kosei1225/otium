# Claude Code に渡すプロンプト

※ ブラウザだけで完結する手順（別紙）で足りる場合は、これは不要です。
※ CLI で一気にやりたい場合のみ使ってください。

---

## 事前にやっておくこと（自分で）

Claude Code は認証情報を扱えないので、**GitHub へのログインだけ先に済ませてください。**

```
gh auth login
```

未インストールなら先に `brew install gh`（Mac）。

---

## プロンプト本文（ここから下をコピーして貼る）

```
Otium という事業コンセプトのランディングページを、GitHub Pages で公開したい。

【前提】
- gh CLI の認証は済ませてある
- 公開するのは index.html 1ファイルだけ。画像はすべて base64 で埋め込み済みなので、
  外部ファイルへの依存はない
- リポジトリは public で構わない（検索エンジン避けの noindex は HTML 側で設定済み）

【手元のファイル】
~/Downloads/otium_github.zip
（中身は index.html と README.md の2ファイル）
※ 実際の保存場所が違ったら聞いてください

【やってほしいこと】
1. 作業用ディレクトリ ~/projects/otium を作り、zip を解凍して中身を置く
2. git init して初回コミット（ブランチ名は main）
3. gh repo create で otium という名前の public リポジトリを作り、push する
4. GitHub Pages を有効化する（main ブランチの / ルートを公開元にする）
5. 公開URLを教えてほしい。反映まで1〜2分かかるはずなので、
   待ってから curl で200が返ってくることを確認してほしい

【やらないでほしいこと】
- index.html の中身は書き換えないでください。デザインは確定済みです
- Jekyll のセットアップやビルド設定は不要です。静的HTML1枚だけです
- パスワードやトークンの入力が必要になったら、自分でやるので止めて教えてください

【あとで自分でやること】
公開後、index.html の中の href="#" を note の URL に差し替えたい。
その箇所が何行目にあるかだけ、最後に教えてください。
```

---

## 補足

**うまくいかない場合、たぶん原因はこの2つのどちらかです。**

**① `gh` が認証されていない**
→ `gh auth status` で確認。未認証なら `gh auth login` をやり直す

**② Pages の反映待ち**
→ 有効化してから最初の1〜2分は 404 が返ります。それは正常です
